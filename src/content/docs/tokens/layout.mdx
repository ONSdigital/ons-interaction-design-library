---
title: "Layout"
description: >
  Our layout tokens provide a clear and consistent way to apply layout styles across our designs. They are based on a layout scale that uses a consistent unit of measurement, allowing for predictable and harmonious layouts.
---
import { Aside } from '@astrojs/starlight/components';
import Token from "../../../components/Token.astro";
import layout from "../../../data/layout.json";

Layout tokens define the core values used to create consistent and flexible page layouts across the ONS website. These tokens include grid settings, container widths, column configurations, and spacing units that align with our design principles.

<Aside>
* Each unit is 4px
* We define the tablet device-width as its portrait orientation.
</Aside>

<table class="token-table">
  <thead>
    <tr>
      <th>Token</th>
      <th>Desktop</th>
      <th>Tablet</th>
      <th>Phone</th>
    </tr>
  </thead>
  <tbody>
    {Object.entries(layout).map(([name, token]) => (
      <tr>
        <td>{ name }</td>
        <td>{ token.desktop.tokenName } <span class="pixel-value">{token.desktop.pixelValue}px</span></td>
        <td>{ token.tablet.tokenName } ({token.tablet.pixelValue}px)</td>
        <td>{ token.phone.tokenName } ({token.phone.pixelValue}px)</td>
      </tr>
    ))}
  </tbody>
</table>